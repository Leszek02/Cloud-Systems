apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-persistent-volume-claim
  namespace: database
spec:
  accessModes:
    - ReadWriteOnce # ReadWriteMany is not supported
  resources:
    requests:
      storage: 10Gi