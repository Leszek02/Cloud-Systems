apiVersion: v1
kind: Service
metadata:
  name: database-service
  namespace: database
spec:
  type: ClusterIP
  selector:
    app: cloud-database
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432